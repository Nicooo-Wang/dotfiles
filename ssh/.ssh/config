# 只让外网SSH走代理，内网不走
Host github.com gitlab.com bitbucket.org *.amazonaws.com
    ProxyCommand nc -X connect -x 127.0.0.1:7890 %h %p

# 或者所有非本地地址都走代理
Host !192.168.* !10.* !127.* !localhost *
    ProxyCommand nc -X connect -x 127.0.0.1:7890 %h %p
